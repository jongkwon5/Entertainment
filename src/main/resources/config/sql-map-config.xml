<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration   PUBLIC "-//mybatis.org//DTD Config 3.0//EN"  "http://mybatis.org/dtd/mybatis-3-config.dtd">

<configuration>
	<properties resource="db.properties"/>
	
	<typeAliases>
		<typeAlias alias="member" type="com.entertain.biz.service.MemberVO"/>
		<typeAlias alias="board" type="com.entertain.biz.board.BoardVO"/>
		<typeAlias alias="comment" type="com.entertain.biz.board.CommentVO"/>
	</typeAliases>
	
	<!-- DataSource설정 (applicationContext.xml에 설정하여 따로 설정 x)-->	
<!-- 	<environments default="development">
		<environment id="development">
			<transactionManager type="JDBC"/>
			<dataSource type="POOLED">
				<property name="driver" value="${jdbc.driver}"/>
				<property name="url" value="${jdbc.url}"/>
				<property name="username" value="${jdbc.username}"/>
				<property name="password" value="${jdbc.password}"/>
			</dataSource>
		</environment>
	</environments>
	 -->
	 <mappers>
	 	<mapper resource="config/mappings/Member-mapping.xml"/>
	 	<mapper resource="config/mappings/Board-mapping.xml"/>
	 </mappers>
</configuration>